<html>
	<head>
	<head>  <link rel="stylesheet" type="text/css" href="/css/main.css"> </head> </head>
		<title> CHITTER </title>
	
			<body>
					<h1> Welcome to Chitter </h1>	
						<% if flash[:errors] && !flash[:errors].empty? %>
						  Sorry, there were the following problems with the form.
						 		 <ul id="errors">
						   			 <% flash[:errors].each do |error| %>
						      			<li><%= error %></li>
						    			<% end %>
						 		 </ul>
						<% end %>
				<%= yield %>
			</body>
</html>